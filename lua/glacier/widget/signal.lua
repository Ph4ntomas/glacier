---Signal emitted by stateful widgets.
---
---@enum glacier.widget.signal
return {
    redraw_needed = "widget::redraw_needed",
    request_focus = "widget::request_focus",
    request_unfocus = "widget::request_unfocus",
}
